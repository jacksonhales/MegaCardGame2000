<Window x:Class="MegaCardGame2000.GameResult"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MegaCardGame2000"
        mc:Ignorable="d"
        Title="GameResult" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="0" Grid.Row="0" >
        <Label x:Name="lblResult" Grid.ColumnSpan="2" HorizontalAlignment="Center">Congratulations!</Label>
            <Label x:Name="lblResultMessage" Grid.ColumnSpan="2" HorizontalAlignment="Center">You have defeated the NPC in battle!</Label>
        </StackPanel>
        <Button Grid.Column="0" Grid.Row="1" HorizontalAlignment="Left" IsDefault="True" x:Name="btnNewGame" Click="btnNewGame_Click" MinWidth="60" Margin="0 10 0 0">New Game</Button>
        <Button Grid.Column="0" Grid.Row="1" HorizontalAlignment="Right" x:Name="btnExit" Click="btnExit_Click"  MinWidth="60" Margin="0 10 0 0">Exit</Button>
    </Grid>
</Window>
